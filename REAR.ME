# Mood Movie Recommender (Flask + MySQL)

## Local setup
1. Create MySQL DB and user (see movies.sql).
2. Copy `.env.example` to `.env` and set your DB credentials.
3. Create & activate virtualenv, then `pip install -r requirements.txt`.
4. Run `python app.py`.
5. Open `http://127.0.0.1:5000/`.

